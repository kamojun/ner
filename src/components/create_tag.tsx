import React from 'react'
import { TagSpan } from './color'
import { RootContext } from '../context'

// const TagCreateTag = () => {
//   const { state, dispatch } = React.useContext(RootContext)
//   return (
//     <TagSpan onClick={() => dispatch({ type: 'newTag', tagname: "NEWTAG" })} color={"rgba(100, 100, 100, 0.3)"}>タグ作成</TagSpan>
//   )
// }

// export default TagCreateTag